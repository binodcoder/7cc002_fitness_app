// import 'package:fitness_app/core/model/Sync_model.dart';
//
// import 'core/usecases/usecase.dart';
// import 'layers/domain_layer/login/usecases/sync.dart';
//
// late SyncModel syncModel;
//
// late final Sync sync;
//
// void callSync() async {
//   final syncResult = await sync(NoParams());
//   syncResult!.fold((failure) {
// // emit(Error(message: _mapFailureToMessage(failure)));
//   }, (sync) {
// // emit(RoutineLoadedSuccessState(routineModelList));
//     syncModel = sync;
//   });
// }
