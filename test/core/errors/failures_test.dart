import 'package:flutter_test/flutter_test.dart';
import 'package:fitness_app/core/errors/failures.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  test('core/errors/failures.dart loads', () {
    expect(true, isTrue);
  });
}
