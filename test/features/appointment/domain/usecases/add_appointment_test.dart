import 'package:flutter_test/flutter_test.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  test('features/appointment/domain/usecases/add_appointment.dart loads', () {
    expect(true, isTrue);
  });
}
