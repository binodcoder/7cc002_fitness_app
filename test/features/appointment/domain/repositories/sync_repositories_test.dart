import 'package:flutter_test/flutter_test.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  test('features/appointment/domain/repositories/sync_repositories.dart loads', () {
    expect(true, isTrue);
  });
}
