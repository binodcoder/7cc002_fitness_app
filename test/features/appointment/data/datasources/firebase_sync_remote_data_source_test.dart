import 'package:flutter_test/flutter_test.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  test('features/appointment/data/datasources/firebase_sync_remote_data_source.dart loads', () {
    expect(true, isTrue);
  });
}
