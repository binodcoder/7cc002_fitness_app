import 'package:flutter_test/flutter_test.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  test('features/live_training/data/models/live_training_model.dart loads', () {
    expect(true, isTrue);
  });
}
