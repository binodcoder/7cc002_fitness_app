import 'package:flutter_test/flutter_test.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  test('features/live_training/data/datasources/live_training_data_source.dart loads', () {
    expect(true, isTrue);
  });
}
