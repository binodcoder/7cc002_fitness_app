import 'package:flutter_test/flutter_test.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  test('features/live_training/domain/repositories/live_training_repositories.dart loads', () {
    expect(true, isTrue);
  });
}
