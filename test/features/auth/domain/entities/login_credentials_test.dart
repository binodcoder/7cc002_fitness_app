import 'package:flutter_test/flutter_test.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  test('features/auth/domain/entities/login_credentials.dart loads', () {
    expect(true, isTrue);
  });
}
