import 'package:flutter_test/flutter_test.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  test('features/walk/domain/usecases/get_walks.dart loads', () {
    expect(true, isTrue);
  });
}
