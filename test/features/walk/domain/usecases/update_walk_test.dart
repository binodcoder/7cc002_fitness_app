import 'package:flutter_test/flutter_test.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  test('features/walk/domain/usecases/update_walk.dart loads', () {
    expect(true, isTrue);
  });
}
