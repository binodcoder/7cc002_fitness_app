import 'package:flutter_test/flutter_test.dart';

void main() {
  TestWidgetsFlutterBinding.ensureInitialized();
  test('features/walk/domain/repositories/walk_repositories.dart loads', () {
    expect(true, isTrue);
  });
}
